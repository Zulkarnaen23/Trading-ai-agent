# OpenAI Platform Setup

Gunakan OpenAI Platform API key hanya di backend.

File:

```text
universal-trading-agent-v6/backend-proxy/.env
```

Isi:

```text
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-5.5
```

Jangan commit `.env` asli ke GitHub.
