# Install di Desktop

1. Deploy folder `web-app` ke hosting.
2. Buka URL PWA di Chrome atau Edge.
3. Klik ikon install di address bar.
4. Aplikasi muncul seperti desktop app.

## Test lokal

```bash
cd web-app
python -m http.server 8080
```

Buka:

```text
http://localhost:8080
```
